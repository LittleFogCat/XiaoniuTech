# Windows 死机排查报告

生成时间：2026-05-06

## 1. 结论

本次问题最可能的主因是显示驱动/图形子系统卡死，而不是典型蓝屏、磁盘坏道或内存/WHEA 硬件报错。

当前最强嫌疑链路是：NVIDIA 驱动 nvlddmkm 异常 + 本机同时存在 OrayIddDriver 虚拟显示驱动 + VS Code/终端这类 GPU 加速应用先出现挂起或崩溃，最终发展为整个用户会话接近假死，只能强制断电重启。

置信度：中高。

## 2. 已确认事实

- 系统环境：Windows 10 Pro，构建 26100；CPU 为 AMD Ryzen 5 3600；物理内存约 32 GB。
- 显卡环境：NVIDIA GeForce GTX 1660 SUPER，驱动版本 32.0.15.9186，驱动日期 2026-01-20。
- 系统中同时存在第二个显示设备：OrayIddDriver Device。这类驱动通常来自远程控制/虚拟显示软件，容易和独显驱动、硬件加速应用形成复杂交互。
- 2026-05-06 00:10:47 发生一次非正常关机；随后在 00:11:49 记录到 Kernel-Power 41，00:12:06 记录到 EventLog 6008。说明系统是“卡死后被强制重启/断电”，不是正常关机。
- 2026-05-03 16:49:10 出现一次 System 日志错误：nvlddmkm，事件 ID 14。这是 NVIDIA 显示驱动相关错误。
- 2026-05-01 05:22:55 出现一次 OpenConsole.exe 崩溃，异常代码 0xc0000005，路径为 D:\software\WezTerm\OpenConsole.exe。
- 2026-04-18 12:09:46 出现一次 wezterm-gui.exe 的 Application Hang 1002，记录为“停止与 Windows 交互并已关闭”。

## 3. 为什么我认为主线是图形/显示栈

- 你的症状不是“应用整体都立即退出”，而是“应用本身还像在运行，但 VS Code、Terminal 卡死，Win+E、Win+R、Ctrl+Alt+Del 无反应”。这更像用户会话层、图形栈或窗口系统被卡住，而不是单一应用崩溃。
- 最近 30 天里，真正命中的硬件/驱动级异常里，唯一明显的高价值错误是 nvlddmkm 事件 14；没有看到成片的 Disk、storahci、stornvme、Ntfs 错误，也没有看到 WHEA CPU/内存/PCIe 硬件错误。
- 终端相关进程已经单独出现过挂起和崩溃，而这类程序通常高度依赖 GPU 加速、字体渲染和窗口合成路径；VS Code 也属于同一类高图形依赖应用。
- 本机还有 OrayIddDriver 虚拟显示驱动，这会增加“独显驱动 + 虚拟显示驱动 + 远控/叠加层/硬件加速应用”组合出问题的概率。

## 4. 已排除或暂不支持的方向

- 不像标准蓝屏：近 30 天没有查到 BugCheck 1001 事件，没有 Minidump，也没有 WER 中的 LiveKernelEvent/BlueScreen 归档。
- 不像磁盘主故障：物理磁盘状态均为 Healthy/OK，卷状态均为 Healthy；日志中也没有连续的磁盘控制器重置、坏块、NTFS 损坏或 volmgr 崩溃证据。
- 不像 WHEA 类硬件错误：没有查到 Microsoft-Windows-WHEA-Logger 事件。
- 不足以证明是单纯的 VS Code 自身问题：日志里并没有 Code.exe 的崩溃证据，反而是终端侧和显示驱动侧更可疑。

## 5. 二次确认结果

为了避免误判，我做了第二轮交叉确认：

- 从可靠性记录看，近期明确异常集中在 wezterm-gui.exe 挂起和 OpenConsole.exe 崩溃，没有出现“视频硬件错误”或蓝屏归档，但这与“卡死后强制断电”并不矛盾，因为这类假死常常来不及产出完整转储。
- 从硬件状态看，三块磁盘当前都显示 Healthy/OK，系统盘卷状态也正常，这与“磁盘故障导致假死”不一致。
- 从异常分布看，最近 30 天没有重复的 WHEA、Disk、Ntfs 错误链；只有 1 次 nvlddmkm 事件 14，但它的价值很高，因为它直接指向显示驱动。

结论不变：最可能是显示驱动/图形栈异常，终端和 VS Code 是先被影响到的高频受害者，而不是唯一根因。

## 6. 本次排查的限制

- DxgKrnl / DWM 诊断日志未启用，因此没有拿到更底层的 GPU 重置细节。
- DISM、sfc、chkdsk 的进一步校验需要管理员权限；当前会话无法提权，因此这部分没有执行成功。
- 现有日志能支持“高概率归因”，但还不能仅凭一次 nvlddmkm 事件就 100% 锁死为某个具体驱动版本缺陷。

## 7. 建议的后续动作

1. 先处理显示驱动链路：使用干净安装方式重装 NVIDIA 驱动，优先尝试一个已知稳定版本，必要时退回到比 32.0.15.9186 更早的稳定分支。
2. 暂时禁用 VS Code、WezTerm、Windows Terminal 的硬件加速，观察 2 到 3 天。如果死机明显消失，基本可以坐实图形加速路径。
3. 暂时停用或卸载 Oray/Sunlogin 的虚拟显示驱动，再观察是否复现；如果你经常开着远控软件，这一步优先级很高。
4. 如果机器装有 GeForce Experience、游戏叠加层、录屏层或第三方悬浮窗，也建议一并关闭做排除。
5. 以管理员身份补跑 DISM /Online /Cleanup-Image /CheckHealth、sfc /verifyonly、chkdsk C: /scan，用于排除系统文件损坏这个低概率分支。
6. 如果后续再次复现，优先记录精确时间点，然后立即检查同一时间窗内的 System 日志；这样能进一步确认是否再次出现 nvlddmkm、Display 或 DxgKrnl 事件。

## 8. 优先级排序后的嫌疑项

1. NVIDIA 显示驱动卡死或图形栈阻塞。
2. OrayIddDriver 虚拟显示驱动与独显驱动/硬件加速应用的兼容性问题。
3. WezTerm/OpenConsole 自身不稳定，但更像“放大器”，不像整机假死的唯一根因。
4. 系统组件或更新残留问题，当前证据不足，仅作为低优先级备选分支。